const add = function(a, b) {
    return a + b;
};

const divide = function(a, b) {
    return a / b;
};

const substract = function(a, b) {
    return a - b;
};

const multiply = function(a, b) {
    return a * b;
};

export { add, divide, substract, multiply}